# Stoke — A股数据获取层

## 项目定位

纯数据层，为量化投研提供 6 大层面数据：行情、研报、信号、新闻、基础数据、公告。
不涉及 LLM 分析、不涉及 GUI，但设计上留了 LLM 调用接口。

## 数据源组合

| 数据源 | 协议/方式 | 限流要求 | 覆盖层面 |
|--------|----------|---------|---------|
| mootdx | TCP（通达信协议） | 无需限流 | 行情（K线+实时）、基础数据(F10) |
| akshare | HTTP（封装东财/同花顺/巨潮/财联社） | 5 秒间隔 | 新闻、研报、公告、涨停信号 |
| 腾讯财经 | HTTP（通过 akshare 封装的 lg 接口） | 3 秒间隔 | PE/PB 估值 |

淘汰的数据源：tushare（收费高且接口受限）、雪球（半成品，大部分方法返回空）。

## 项目结构

```
stoke/
├── stoke/                    # Python 包
│   ├── __init__.py
│   ├── config.py             # 全局配置
│   ├── rate_limiter.py       # 统一限流器
│   └── sources/              # 数据源适配层
│       ├── __init__.py
│       ├── mootdx_source.py
│       ├── akshare_source.py
│       └── tencent_source.py
├── tests/                    # 测试
├── pyproject.toml
└── CLAUDE.md
```

## 编码规范

- **Python 3.11+**，UTF-8 编码，中文注释
- 包管理用 `uv`（`uv add` / `uv run`），不用 pip
- 代码加注释——用户是编程初学者
- 先讨论思路，确认后再写代码
- 修改前先理解原有逻辑，不乱改
- 不确定时反问，不猜测执行
- 所有沟通用中文
- 环境：Mac M4 芯片

## 核心原则

1. **限流是铁律** — 每个数据源必须遵守其平台的调用间隔，不可频繁请求导致封 IP
2. **保持简单** — 不做缓存、不做过度抽象、三个 Source 各自独立不强制统一接口
3. **health_check() 必实现** — 每个 Source 都要能快速验证连通性
4. **返回 DataFrame** — 行情/K线/公告用 DataFrame，纯文本类（如新闻、财联社电报）可用 list[dict]
